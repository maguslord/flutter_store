import 'package:flutter/material.dart'; 

class Search extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return   Scaffold(
      
      body: Container(

        child: Text('search Page'),
        alignment: Alignment.center,
        color: const Color.fromARGB(255, 113, 159, 197),
      
      ),
    
    );
  }
}